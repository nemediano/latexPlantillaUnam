\ProvidesPackage{Thesis}

% Usamos polyglosia en vez de babel por ser mas moderno. Definimos dos lenguajes
\usepackage{polyglossia}
\setmainlanguage{spanish}
\setotherlanguage[variant=american]{english}

\usepackage{graphicx} % Poder incluir imágenes
\usepackage{subcaption} % Poder usar subfiguras
\usepackage[normalem]{ulem}
% Si se requiere una renglón con múltiples columnas en la tablas.
\usepackage{multicol}
% Una columna con múltiples renglones en las tablas
\usepackage{multirow}

% Símbolos refinados de matemáticas
\usepackage{amsmath}
% Símbolos extras de matemáticas
\usepackage{amssymb}
% Espacios después de ciertos símbolos
\usepackage{textcomp} 

% Tablas con look profesional
\usepackage{booktabs}
\newcommand{\ra}[1]{\renewcommand{\arraystretch}{#1}}

% Antes de cualquier cosa define el espacio (la pagina y los margenes)
\usepackage[letterpaper,margin=3cm]{geometry}

% Los encabezados y pies de página
\usepackage{fancyhdr}
% Configura la pagina `fancy`
\setlength{\headheight}{16pt}
\pagestyle{fancy}
\fancyhf{} % Quita todo
% Pon lo siguiente
\fancyhead[LE,RO]{\thepage}
\fancyhead[RE]{\textsc{\nouppercase{\leftmark}}}
\fancyhead[LO]{\textsc{\nouppercase{\rightmark}}}
% Configura la pagina vacía (La que se ocupa en el índice, dedicatoria, etc)
\fancypagestyle{plain}{ %
\fancyhf{} % Quita todo
\renewcommand{\headrulewidth}{0pt} % También las lineas
\renewcommand{\footrulewidth}{0pt}
}

\setlength{\parindent}{0cm}
\setlength{\parskip}{1.5ex}

\usepackage{amsthm}
% Algunas definiciones de nuevos entornos que podrían ser útiles
\theoremstyle{plain}
\newtheorem{definition}{Definición}[section]
\newtheorem{example}{Ejemplo}[section]
\newtheorem{teo}{Teorema}
\newtheorem*{teoSN}{Teorema}

% Para la versión final
\usepackage[
final,
unicode,
colorlinks=true,
citecolor=blue,
linkcolor=blue,
urlcolor=blue,
plainpages=false,
pdfpagelabels=true,
pdfauthor={José Doroteo Arango Arámbula},
pdftitle={Mi trabajo de titulo muy largo y muy especifico como debe ser en una tesis}
]{hyperref}
% Si se usa draft
% \usepackage[draft,pdftex,unicode,colorlinks=true,citecolor=blue,linkcolor=blue,plainpages=false,
% pdfpagelabels=true,pdfauthor={José Doroteo Arango Arámbula}{hyperref}

\setcounter{tocdepth}{4}
\setcounter{secnumdepth}{4}
% Para poner el espacio de entrelineado
\usepackage{setspace} 
% Espacio de 1.5 lineas
\onehalfspacing

% Este paquete me deja personalizar los pies de las figuras y las tablas
\usepackage[margin=2cm,font=footnotesize,labelfont=bf]{caption}

% Estos dos paquetes me dejan poner pseudocódigos se usan en conjunto
\usepackage{algpseudocode}
% Ambiente flotante para algoritmos
\usepackage{algorithm}
% Que los comentarios en un pseudocódigo sean similares a C++
%\algrenewcommand{\algorithmiccomment}[1]{\hskip3em$//$ #1}
%Poniendo los encabezados de los algoritmos en español
\floatname{algorithm}{Algoritmo}
\renewcommand{\algorithmicrequire}{\textbf{Entradas:}}
\renewcommand{\algorithmicensure}{\textbf{Salidas:}}
\renewcommand{\listalgorithmname}{Índice de algoritmos}

% Poner urls
\usepackage{url}
% Barras de cambio
%\usepackage[color]{changebar} 
%\cbcolor{blue} %Color de barra de cambio

% Usar negritas en entorno de matemáticas
\usepackage{bm}
\usepackage{mathrsfs}

% Para usar las comillas
\usepackage{dirtytalk} 

% Para poner epígrafes
\usepackage{epigraph} 
% Si no ajustamos, el espacio para un epígrafe es muy poco
\setlength{\epigraphwidth}{0.6\textwidth} 
% Para insertar código fuente
\usepackage[newfloat=true]{minted} 
% Requieres compilar usando:
% $xelatex -shell-escape input.tex
% Para compilar todo el proyecto se necesita:
% 1 XeLaTeX
% 2 Biber 
% 3 XeLaTex
% 4 XeLateX
% 5 ViewPDF

% Definir un entorno no flotante para códigos que abarcan varias páginas
% Tomé el hack de aqui
% https://tex.stackexchange.com/questions/33866/algorithm-tag-and-page-break
% https://tex.stackexchange.com/questions/254044/caption-and-label-on-minted-code
\DeclareCaptionFormat{mitedFormat}{%
    \textbf{#1#2}#3}
\DeclareCaptionStyle{minetdStyle}{skip=0cm,width=.85\textwidth,justification=centering,
  font=footnotesize,singlelinecheck=off,format=mitedFormat,labelsep=space}
\newenvironment{mintedCode}{\captionsetup{type=listing,style=minetdStyle}}{}

% Configurar minted para todo el documento
\SetupFloatingEnvironment{listing}{name=Listado}
% Configurar minted para el lenguaje C++
\setminted[cpp]{frame=lines,framesep=0.25cm,baselinestretch=1,fontsize=\scriptsize,breaklines}
% Excepto si es dento de un párrafo
\setmintedinline[cpp]{fontsize=auto}

% Por que si no esta presente justo caqui polyglosia tira un warning
\usepackage{csquotes} 
% Para poner los logo de los programas de LaTeX, por ejemplo \hologo{XeLaTeX}
\usepackage{hologo}
